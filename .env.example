# AI Provider Configuration
# Using Hugging Face (German models for job extraction)
# German text generation model (for analyzing job postings)
# Veronika-T/mistral-german-7b is a powerful German-optimized model
HUGGINGFACE_MODEL=Veronika-T/mistral-german-7b
# German embedding model (for semantic search and analysis)
# deutsche-telekom/gbert-large-paraphrase-cosine is optimized for German text
HUGGINGFACE_EMBEDDING_MODEL=deutsche-telekom/gbert-large-paraphrase-cosine
# Hugging Face API token (optional, only needed for gated models)
# Get your token from https://huggingface.co/settings/tokens
HUGGINGFACE_API_TOKEN=

# Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG_MODE=True

# Excel File Path
# Path to the Excel file containing company data
EXCEL_FILE_PATH=data/Landratsamt.xlsx

# JSON Output Directory
# Directory where processed job results will be saved as JSON files
JSON_OUTPUT_DIR=data/output

# Processing Configuration
MAX_CONCURRENT_REQUESTS=5
REQUEST_TIMEOUT=30

# AI Rate Limiting
# Rate limiting is disabled for local Hugging Face models (no delays between calls)
# AI_RATE_LIMIT_DELAY is deprecated and no longer used

# Frontend Callback Configuration (DEPRECATED)
# This setting is kept for backward compatibility but is no longer used
# Results are now saved as JSON files in JSON_OUTPUT_DIR instead of being POSTed
# FRONTEND_CALLBACK_URL=http://localhost:5173/api/jobs/callback
